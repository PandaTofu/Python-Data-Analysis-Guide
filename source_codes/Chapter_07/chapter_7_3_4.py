import numpy as np

import pandas as pd

data = np.concatenate([np.random.normal(3, 1.5, 6),
                       np.random.normal(11, 0.5, 6),
                       np.random.normal(20, 2.1, 6)])

list('AAAAAABBBBBBCCCCCC'),

df

df.groupby('c1').agg(['mean', 'std'])

x['c2'].mean() > 10)

x['c2'].std() > 1.)

(x['c2'].std() > 1.) and ((x['c3'] < 0).sum() > 3))

